import React from 'react';
import { shallow } from 'enzyme';

describe('Should render with success', () => {
  test('renders without crashing', () => {
    expect(1).toBe(1);
    // ReactDOM.render(<Index />, div);
    // ReactDOM.unmountComponentAtNode(div);
  });
});

// describe('Oi!', () => {
//   test('xD', () => {
//     expect(1).toBe(1);
//   });
// });
